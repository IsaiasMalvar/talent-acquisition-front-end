import React from "react";

const CreateRequestPage = (): React.ReactElement => {
    return (
        <section>
            <h1 className="uppercase">Create New R</h1>
        </section>
    );
};

export default CreateRequestPage;
